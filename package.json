{
  "name": "teacherai-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "prisma:push": "npm --prefix web run prisma db push",
    "prisma:generate": "npm --prefix web run prisma generate"
  }
}
